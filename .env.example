NODE_ENV=development
PORT=3000
DATABASE_PATH=./db/database.sqlite

# Root directory for file browser
# This is the default starting directory when browsing for working directories
# Use ~ for home directory, or absolute paths like /Users/username/code
ROOT_WORKING_DIR=~/code

# Maximum character limit for the message input field
# Default: 5000 characters
INPUT_LENGTH=5000

# Terminal command for Deep Dive feature
# Default: "node wingman-cli.js" (relative to wingman path)
# If you have wingman aliased in your shell, you can use: "wingman"
TERMINALCMD="node wingman-cli.js"

# Trigger API Authentication Token
# Used to authenticate external systems calling the /api/triggers endpoint
TRIGGER_TOKEN=your_secure_trigger_token_here

# Wingman RTB MCP Server Security Settings
# Comma-separated list of allowed webhook domains/patterns
# Use wildcards (*) for subdomains: *.example.com
# Use port patterns: localhost:*
WINGMAN_RTB_WEBHOOK_WHITELIST=*localhost*,webhook.site*

# Add your environment variables here
# API_KEY=your_api_key_here
# SECRET_KEY=your_secret_key_here
